# 📋 Next.js Blog 接单升级计划

> **目标**：将现有博客项目打造为接单级 Portfolio Demo，覆盖 TypeScript 迁移、Prisma + Supabase 数据库集成、平台注册与 Profile 优化三大模块。
>
> **预计总耗时**：5-6 天
>
> **当前项目**：`next14-blog`（Next.js 14 App Router + MDX + TailwindCSS + Framer Motion）

---

## 目录

- [Day 1-3：TypeScript 迁移](#day-1-3typescript-迁移)
- [Day 4-5：Prisma + Supabase 集成](#day-4-5prisma--supabase-集成)
- [Day 6：平台注册 + Profile 优化](#day-6平台注册--profile-优化)
- [附录：检查清单](#附录检查清单)

---

## Day 1-3：TypeScript 迁移

### 背景

项目当前为纯 JavaScript（33 个源文件），零 TypeScript 代码。Upwork/Fiverr 上 **80%+ 的 Next.js 项目要求 TypeScript**，这是接单最大的技术门槛。

### 第一天上午：环境搭建与配置

#### 1.1 安装依赖

```bash
npm install --save-dev typescript @types/react @types/react-dom @types/node
```

#### 1.2 创建 `tsconfig.json`

```json
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

#### 1.3 删除 `jsconfig.json`（被 `tsconfig.json` 替代）

#### 1.4 添加 `.next` 到 `.gitignore`（如尚未忽略）

---

### 第一天下午：类型定义文件

#### 2.1 创建 `types/index.ts` — 核心数据类型

```typescript
// ============ MDX 文章 ============
export interface PostFrontmatter {
  title: string;
  description: string;
  date: string;
  tags: string[];
}

export interface Post {
  slug: string;
  frontmatter: PostFrontmatter;
  content: string;
  readingTime?: number;
}

// ============ GitHub 项目 ============
export interface Repo {
  id: number;
  name: string;
  title?: string;          // fallback 使用 db.json 时的字段名
  description: string | null;
  stargazers_count: number;
  html_url: string;
  language: string | null;
}

// ============ 分页 ============
export interface PaginationProps {
  currentPage: number;
  totalPages: number;
  basePath: string;
}

// ============ i18n ============
export type Locale = 'en' | 'zh';

// 翻译字典 — 用于约束翻译键结构
export interface TranslationDict {
  nav: {
    home: string;
    blog: string;
    about: string;
    projects: string;
    photos: string;
  };
  home: {
    title: string;
    subtitle: string;
    readMore: string;
    latestPosts: string;
    allPosts: string;
  };
  about: {
    title: string;
    description: string;
    viewProjects: string;
  };
  blog: {
    title: string;
    description: string;
    noPosts: string;
    previous: string;
    next: string;
    page: string;
  };
  projects: {
    title: string;
    description: string;
    loading: string;
    error: string;
  };
  photos: {
    title: string;
    description: string;
  };
  '404': {
    title: string;
    description: string;
    goHome: string;
  };
}

// ============ 主题 ============
export type Theme = 'dark' | 'light';

// ============ Cookie 状态 ============
export interface AppCookies {
  theme?: Theme;
  lang?: Locale;
}

// ============ 页面导航 ============
export interface NavItem {
  href: string;
  label: string;
  external?: boolean;
}
```

#### 2.2 创建 `types/mdx.d.ts` — MDX 模块声明

```typescript
declare module '*.mdx' {
  import type { MDXProps } from 'mdx/types';
  let MDXComponent: (props: MDXProps) => JSX.Element;
  export default MDXComponent;
  export const frontmatter: import('./index').PostFrontmatter;
}
```

#### 2.3 创建 `types/env.d.ts` — 环境变量声明（预留）

```typescript
declare namespace NodeJS {
  interface ProcessEnv {
    NEXT_PUBLIC_SUPABASE_URL?: string;
    NEXT_PUBLIC_SUPABASE_ANON_KEY?: string;
    DATABASE_URL?: string;
    GITHUB_TOKEN?: string;
  }
}
```

---

### 第二天：核心文件逐批迁移

> 迁移策略：**从底层到上层，从无依赖到有依赖**，确保每批次迁移后项目仍可运行。

#### 第一批：工具库（lib/）— 约 1 小时

| 文件 | 重命名为 | 改动点 |
|------|---------|--------|
| `lib/posts.js` | `lib/posts.ts` | 添加 `Post`、`PostFrontmatter` 类型；`compileMDX` 泛型标注 |
| `lib/i18n.js` | `lib/i18n.ts` | 添加 `Locale`、`TranslationDict` 类型；翻译对象标注类型 |

**`lib/posts.ts` 关键类型标注：**

```typescript
import type { Post, PostFrontmatter } from '@/types';

export async function getPosts(
  options?: { page?: number; limit?: number; tag?: string }
): Promise<{ posts: Post[]; total: number; totalPages: number }> {
  // ...
}

export async function getPostBySlug(slug: string): Promise<Post | null> {
  // ...
}

export async function getAllTags(): Promise<string[]> {
  // ...
}
```

**`lib/i18n.ts` 关键类型标注：**

```typescript
import type { Locale, TranslationDict } from '@/types';

const translations: Record<Locale, TranslationDict> = { en: { ... }, zh: { ... } };

export function getTranslation(
  lang: Locale, 
  namespace: string, 
  key: string
): string {
  // ...
}
```

#### 第二批：自定义 Hooks（hooks/）— 约 1 小时

| 文件 | 重命名为 | 改动点 |
|------|---------|--------|
| `hooks/use-server-dark-mode.js` | `hooks/use-server-dark-mode.ts` | 添加 `Theme` 类型 |
| `hooks/use-dark-mode.js` | `hooks/use-dark-mode.ts` | 添加 `Theme` 类型 |
| `hooks/use-server-language.js` | `hooks/use-server-language.ts` | 添加 `Locale` 类型 |
| `hooks/use-language.js` | `hooks/use-language.ts` | 添加 `Locale` 类型 |

#### 第三批：组件（components/）— 约 2-3 小时

| 文件 | 重命名为 | 改动点 |
|------|---------|--------|
| `components/h1.js` | `components/h1.tsx` | Props 接口定义 |
| `components/card.js` | `components/card.tsx` | Post 类型、Props 接口 |
| `components/counter.js` | `components/counter.tsx` | Props 接口 |
| `components/dark-mode.js` | `components/dark-mode.tsx` | 无外部类型依赖 |
| `components/header.js` | `components/header.tsx` | 无外部类型依赖 |
| `components/navigation.js` | `components/navigation.tsx` | NavItem 类型 |
| `components/language-toggle.js` | `components/language-toggle.tsx` | 无外部类型依赖 |
| `components/pagination.js` | `components/pagination.tsx` | PaginationProps 类型 |
| `components/page-transition.js` | `components/page-transition.tsx` | motion 组件类型 |
| `components/chatbot.js` | `components/chatbot.tsx` | Script 组件类型 |

**典型 Props 接口示例：**

```typescript
interface CardProps {
  post: Post;
  locale?: Locale;
}

interface PaginationProps {
  currentPage: number;
  totalPages: number;
  basePath: string;
}
```

#### 第四批：页面路由（app/）— 约 3-4 小时

| 文件 | 重命名为 | 改动点 |
|------|---------|--------|
| `app/layout.js` | `app/layout.tsx` | Metadata 类型标注 |
| `app/page.js` | `app/page.tsx` | Post[] 类型 |
| `app/blog/page.js` | `app/blog/page.tsx` | 分页参数类型 |
| `app/blog/[slug]/page.js` | `app/blog/[slug]/page.tsx` | generateStaticParams |
| `app/about/page.js` | `app/about/page.tsx` | 翻译函数类型 |
| `app/about/layout.js` | `app/about/layout.tsx` | 基本无改动 |
| `app/about/projects/page.js` | `app/about/projects/page.tsx` | 基本无改动 |
| `app/about/projects/components/project-list.js` | `app/about/projects/components/project-list.tsx` | Repo 类型 |
| `app/about/projects/components/project-list-loading.js` | `app/about/projects/components/project-list-loading.tsx` | 基本无改动 |
| `app/photos/page.js` | `app/photos/page.tsx` | StaticImageData 类型 |
| `app/not-found.js` | `app/not-found.tsx` | 基本无改动 |
| `app/global-error.js` | `app/global-error.tsx` | Error 类型 |
| `app/about/error.js` | `app/about/error.tsx` | Error 类型 |
| `app/blog/[slug]/not-found.js` | `app/blog/[slug]/not-found.tsx` | 基本无改动 |
| `app/blog/[slug]/opengraph-image.js` | `app/blog/[slug]/opengraph-image.tsx` | ImageResponse 类型 |

---

### 第三天：配置文件迁移 + 修复 + 验证

#### 配置文件迁移（约 30 分钟）

| 文件 | 重命名为 | 改动点 |
|------|---------|--------|
| `next.config.js` | `next.config.ts` | 使用 `defineConfig` |
| `tailwind.config.js` | `tailwind.config.ts` | 使用 `Config` 类型 |
| `postcss.config.js` | `postcss.config.mjs` | 保持 JS（PostCSS 推荐） |
| `mdx-components.jsx` | `mdx-components.tsx` | MDXComponents 类型 |

**`next.config.ts` 示例：**

```typescript
import type { NextConfig } from 'next';
import createMDX from '@next/mdx';

const nextConfig: NextConfig = {
  pageExtensions: ['js', 'jsx', 'mdx', 'ts', 'tsx'],
};

const withMDX = createMDX();
export default withMDX(nextConfig);
```

#### 类型错误修复（约 2-3 小时）

常见需要修复的类型问题：

1. **`fs.readFileSync` / `path.join`** → 已由 `@types/node` 覆盖
2. **`any` 类型消除** → 逐步替换为具体类型，实在推断不出的可以保留 `unknown`
3. **`framer-motion` 组件 Props** → motion 组件可能需要 `as any` 或安装 `@types/framer-motion`
4. **`react-cookie` 的 `useCookies`** → 需要安装 `@types/react-cookie`
5. **`next-mdx-remote/rsc` 的 `compileMDX`** → 泛型参数标注
6. **`useSearchParams` / `usePathname`** → 从 `next/navigation` 导入，类型自动
7. **`cookies()` / `headers()`** → 从 `next/headers` 导入，类型自动

#### 最终验证（约 30 分钟）

```bash
# 类型检查（不输出文件）
npx tsc --noEmit

# 构建验证
npm run build

# 开发环境验证
npm run dev
```

#### 迁移完成标志

- [x] 零 `.js` / `.jsx` 源文件（配置文件除外可选保留）
- [x] `npx tsc --noEmit` 通过
- [x] `npm run build` 成功
- [x] `npm run dev` 所有页面正常渲染
- [x] 国际化、暗色模式、Chatbot、分页等功能正常
- [x] Git commit：`feat: migrate to TypeScript`

---

## Day 4-5：Prisma + Supabase 集成

### 背景

当前数据源：
- **博客内容**：文件系统 MDX（`content/` 目录）→ 改为 Supabase PostgreSQL
- **项目列表**：GitHub API + `db.json` fallback → 改为 Supabase PostgreSQL
- **状态管理**：`react-cookie`（theme/lang）→ 保持不变

目标：展示企业级全栈能力，实现真实的数据库 CRUD。

### 第四天上午：Supabase 项目创建与数据库设计

#### 4.1 注册 Supabase

1. 访问 [supabase.com](https://supabase.com) 注册账号
2. 创建新项目，选择 **亚太地区**（ap-southeast-1）
3. 获取 `DATABASE_URL`、`NEXT_PUBLIC_SUPABASE_URL`、`NEXT_PUBLIC_SUPABASE_ANON_KEY`
4. 将以上三个值写入 `.env.local`

```
DATABASE_URL="postgresql://postgres.xxx:password@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres"
NEXT_PUBLIC_SUPABASE_URL="https://xxxxx.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJhbGciOi..."
```

#### 4.2 安装 Prisma 依赖

```bash
npm install prisma @prisma/client
npm install --save-dev @types/prisma
npx prisma init
```

#### 4.3 设计数据库 Schema

创建 `prisma/schema.prisma`：

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ============ 文章模型 ============
model Post {
  id          String    @id @default(cuid())
  slug        String    @unique
  title       String
  description String
  content     String                      // MDX 源码
  date        DateTime
  published   Boolean   @default(true)
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  tags        PostTag[]
}

// ============ 标签模型 ============
model Tag {
  id    String    @id @default(cuid())
  name  String    @unique
  slug  String    @unique
  posts PostTag[]
}

// ============ 文章-标签 多对多关联 ============
model PostTag {
  postId String
  tagId  String
  post   Post @relation(fields: [postId], references: [id], onDelete: Cascade)
  tag    Tag  @relation(fields: [tagId], references: [id], onDelete: Cascade)

  @@id([postId, tagId])
}

// ============ 项目/作品模型 ============
model Project {
  id              String    @id @default(cuid())
  name            String
  description     String?
  url             String?
  language        String?
  stargazersCount Int       @default(0)
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
}

// ============ 用户模型（预留，后续接入认证） ============
model User {
  id        String   @id @default(cuid())
  email     String   @unique
  name      String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
```

#### 4.4 执行数据库迁移

```bash
npx prisma migrate dev --name init
npx prisma generate
```

#### 4.5 创建 Prisma Client 单例

创建 `lib/prisma.ts`：

```typescript
import { PrismaClient } from '@prisma/client';

const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined;
};

export const prisma = globalForPrisma.prisma ?? new PrismaClient();

if (process.env.NODE_ENV !== 'production') {
  globalForPrisma.prisma = prisma;
}
```

---

### 第四天下午：重写数据访问层

#### 5.1 重写 `lib/posts.ts` — 从文件系统改为 Prisma

```typescript
import { prisma } from './prisma';
import type { Post, PostFrontmatter } from '@/types';

export async function getPosts(options?: {
  page?: number;
  limit?: number;
  tag?: string;
}): Promise<{ posts: Post[]; total: number; totalPages: number }> {
  const page = options?.page ?? 1;
  const limit = options?.limit ?? 5;

  const where = options?.tag
    ? { tags: { some: { tag: { slug: options.tag } } } }
    : {};

  const [posts, total] = await Promise.all([
    prisma.post.findMany({
      where: { ...where, published: true },
      include: { tags: { include: { tag: true } } },
      orderBy: { date: 'desc' },
      skip: (page - 1) * limit,
      take: limit,
    }),
    prisma.post.count({ where: { ...where, published: true } }),
  ]);

  return {
    posts: posts.map(mapPrismaPostToPost),
    total,
    totalPages: Math.ceil(total / limit),
  };
}

export async function getPostBySlug(slug: string): Promise<Post | null> {
  const post = await prisma.post.findUnique({
    where: { slug },
    include: { tags: { include: { tag: true } } },
  });

  if (!post) return null;
  return mapPrismaPostToPost(post);
}

export async function getAllTags(): Promise<string[]> {
  const tags = await prisma.tag.findMany({
    orderBy: { name: 'asc' },
  });
  return tags.map((t) => t.name);
}

// 辅助函数：Prisma 模型 → 前端类型
function mapPrismaPostToPost(
  prismaPost: any  // 复杂关联类型，可以后续优化
): Post {
  return {
    slug: prismaPost.slug,
    frontmatter: {
      title: prismaPost.title,
      description: prismaPost.description,
      date: prismaPost.date.toISOString().split('T')[0],
      tags: prismaPost.tags.map((pt: any) => pt.tag.name),
    },
    content: prismaPost.content,
  };
}
```

#### 5.2 重写 `project-list.tsx` — 从 GitHub API 改为 Prisma

```typescript
// 替换原有的 fetch GitHub API 逻辑
const projects = await prisma.project.findMany({
  orderBy: { stargazersCount: 'desc' },
});
```

#### 5.3 创建种子脚本 — 迁移现有内容到数据库

创建 `prisma/seed.ts`：

```typescript
import { PrismaClient } from '@prisma/client';
import fs from 'fs';
import path from 'path';

const prisma = new PrismaClient();

async function main() {
  // 1. 种子标签
  const tagNames = [
    'javascript', 'typescript', 'css', 'flexbox', 'react',
    'hooks', 'nodejs', 'backend', 'vue', 'frontend',
    'html', 'web design', 'project', 'nextjs', 'blog',
    'portfolio', 'tailwindcss', 'mdx',
  ];

  for (const name of tagNames) {
    await prisma.tag.upsert({
      where: { name },
      update: {},
      create: { name, slug: name.toLowerCase().replace(/\s+/g, '-') },
    });
  }

  // 2. 种子文章 (从 content/ 目录读取 MDX 文件的 frontmatter)
  const contentDir = path.join(process.cwd(), 'content');
  const files = fs.readdirSync(contentDir).filter((f) => f.endsWith('.mdx'));

  for (const file of files) {
    const raw = fs.readFileSync(path.join(contentDir, file), 'utf-8');
    // 简单解析 frontmatter（或使用 gray-matter 库）
    // ... 解析逻辑
    // await prisma.post.create({ ... });
  }

  // 3. 种子项目 (从 db.json)
  const dbJson = JSON.parse(
    fs.readFileSync(path.join(process.cwd(), 'db.json'), 'utf-8')
  );

  for (const repo of dbJson.repos) {
    await prisma.project.upsert({
      where: { id: String(repo.id) },
      update: {},
      create: {
        id: String(repo.id),
        name: repo.title,
        description: repo.description,
        stargazersCount: repo.stargazers_count,
      },
    });
  }

  console.log('✅ Seed completed!');
}

main()
  .catch((e) => {
    console.error(e);
    process.exit(1);
  })
  .finally(async () => {
    await prisma.$disconnect();
  });
```

在 `package.json` 中添加：

```json
{
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  }
}
```

运行：

```bash
npx prisma db seed
```

---

### 第五天上午：API Route 开发与认证预留

#### 6.1 创建 API Routes（展示 Route Handler 能力）

创建 `app/api/posts/route.ts`：

```typescript
import { NextRequest, NextResponse } from 'next/server';
import { prisma } from '@/lib/prisma';

export async function GET(request: NextRequest) {
  const searchParams = request.nextUrl.searchParams;
  const page = parseInt(searchParams.get('page') ?? '1');
  const limit = parseInt(searchParams.get('limit') ?? '10');
  const tag = searchParams.get('tag');

  const where: any = { published: true };
  if (tag) {
    where.tags = { some: { tag: { slug: tag } } };
  }

  const [posts, total] = await Promise.all([
    prisma.post.findMany({
      where,
      include: { tags: { include: { tag: true } } },
      orderBy: { date: 'desc' },
      skip: (page - 1) * limit,
      take: limit,
    }),
    prisma.post.count({ where }),
  ]);

  return NextResponse.json({
    posts: posts.map((p) => ({
      id: p.id,
      slug: p.slug,
      title: p.title,
      description: p.description,
      date: p.date,
      tags: p.tags.map((pt) => pt.tag.name),
    })),
    total,
    page,
    totalPages: Math.ceil(total / limit),
  });
}
```

#### 6.2 创建 Supabase 客户端（可选，为后续认证做准备）

创建 `lib/supabase.ts`：

```typescript
import { createClient } from '@supabase/supabase-js';

const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!;
const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!;

export const supabase = createClient(supabaseUrl, supabaseAnonKey);
```

---

### 第五天下午：清理与部署验证

#### 7.1 清理旧依赖

```bash
npm uninstall json-server
rm db.json  # 数据已迁移到 Supabase
```

#### 7.2 更新 `package.json` scripts

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "npx prisma generate && next build",
    "start": "next start",
    "lint": "next lint",
    "db:migrate": "npx prisma migrate dev",
    "db:seed": "npx prisma db seed",
    "db:studio": "npx prisma studio",
    "db:push": "npx prisma db push",
    "postinstall": "npx prisma generate"
  }
}
```

#### 7.3 Vercel 部署配置

在 Vercel 项目设置中添加环境变量：
- `DATABASE_URL`
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`

在 `package.json` 中确保 `postinstall` 脚本存在，Vercel 每次部署会自动 `prisma generate`。

#### 7.4 验证清单

- [x] `npx tsc --noEmit` 通过
- [x] `npm run build` 成功
- [x] 首页文章列表从 Supabase 加载
- [x] 文章详情页正常渲染
- [x] 标签筛选功能正常
- [x] 分页功能正常
- [x] Projects 页面从 Supabase 加载
- [x] API Route `/api/posts` 返回正确 JSON
- [x] Vercel 部署成功
- [x] Git commit：`feat: migrate to Prisma + Supabase`

---

## Day 6：平台注册 + Profile 优化

### 目标

在 Upwork、Fiverr、程序员客栈等平台建立专业接单形象，写好 Profile 和作品展示。

---

### 第六天上午：平台注册

#### 8.1 Upwork 注册（国际接单主战场）

**注册步骤**：
1. 访问 [upwork.com](https://www.upwork.com) 注册
2. 填写个人信息
3. **关键：Profile 标题** → 决定客户是否点进来看

**标题模板**：
```
Full-Stack Next.js Developer | TypeScript, React, Prisma, Supabase
```

**Overview（个人简介）模板**：

```
I'm a full-stack developer specializing in modern web applications built with 
Next.js, TypeScript, and PostgreSQL.

📌 What I can do for you:
- Build blazing-fast, SEO-optimized websites with Next.js App Router
- Develop full-stack SaaS applications with Prisma + Supabase
- Implement responsive, animated UI with Tailwind CSS + Framer Motion
- Set up i18n (internationalization) for multi-language sites
- Create custom MDX-based blogs and CMS-driven sites
- Deploy and maintain applications on Vercel

🛠 Tech Stack:
Next.js 14+ · TypeScript · React · Prisma · Supabase/PostgreSQL
Tailwind CSS · Framer Motion · MDX · Vercel

📎 Portfolio: [你的 Vercel 部署链接]
📎 GitHub: [你的 GitHub 主页]

Let's build something great together! Feel free to reach out.
```

**技能标签（Skills）**：
`Next.js`, `TypeScript`, `React`, `Prisma`, `Supabase`, `PostgreSQL`, `Tailwind CSS`, `Framer Motion`, `MDX`, `Vercel`, `i18n`, `Web Development`, `Front-End Development`, `Full-Stack Development`

**时薪设定**：
- 新手期：$25-35/hr
- 积累 3-5 个好评后：$40-60/hr
- 长期目标：$80+/hr

**头像建议**：清晰正面照，背景简洁，微笑

---

#### 8.2 Fiverr 注册（固定价格 Gig）

**Gig 标题模板**：

```
I will build a modern Next.js website with TypeScript and Tailwind CSS
```

**Gig Packages**：

| Package | 价格 | 内容 |
|---------|------|------|
| **Basic** | $100 | 单页 Landing Page，Next.js + Tailwind，响应式，1 次修改 |
| **Standard** | $300 | 3-5 页网站，含 Blog/CMS，暗色模式，SEO 优化 |
| **Premium** | $800+ | 全栈应用，Prisma + Supabase，认证，i18n，动画，Vercel 部署 |

**Gig 封面图**：用你的博客截图 + 工具制作漂亮的封面

---

#### 8.3 程序员客栈 / 电鸭社区（国内）

- **程序员客栈**：[proginn.com](https://www.proginn.com)
- **电鸭社区**：[eleduck.com](https://eleduck.com)

国内平台的标题可以更直接：
```
Next.js 全栈开发者 | TypeScript + Prisma + Supabase | 6年经验
```

---

### 第六天下午：作品集与 Profile 优化

#### 9.1 优化线上 Demo

确保 Vercel 部署的博客满足：

- [x] 首页加载速度 < 3 秒（Vercel Speed Insights 监控）
- [x] Lighthouse 评分 > 90（Performance / Accessibility / Best Practices / SEO）
- [x] 自定义域名（可选但加分，如 `yourname.dev`）
- [x] 至少 5 篇高质量博客文章
- [x] About 页面写好个人介绍
- [x] Projects 页面展示 3-5 个作品
- [x] 暗色/亮色模式正常工作
- [x] 中英文切换正常

#### 9.2 GitHub Profile 优化

- [x] 设置 GitHub Profile README（`username/username` 仓库）
- [x] Pin 博客项目到首页
- [x] 写清楚项目技术栈和功能
- [x] README 加入 Demo 链接和截图

**GitHub Profile README 模板**：

```markdown
## Hi there 👋 I'm [Your Name]

Full-Stack Developer | Next.js · TypeScript · Supabase

### 🔥 Featured Project

[![Next.js Blog](screenshot.png)](https://your-demo.vercel.app)

**Next.js 14 Full-Stack Blog**
- 🚀 Next.js 14 App Router + TypeScript
- 🗄️ Prisma + Supabase PostgreSQL
- 🌐 i18n (EN/ZH) internationalization
- 🎨 Tailwind CSS + Framer Motion animations
- 🌙 Dark mode with SSR-safe persistence
- 🤖 Built-in AI chatbot

[Live Demo](https://your-demo.vercel.app) · [Source Code](https://github.com/...)

### 🛠 Tech Stack
`Next.js` `TypeScript` `React` `Prisma` `Supabase` `PostgreSQL` `Tailwind CSS` `Framer Motion` `Vercel`
```

#### 9.3 准备 Proposal 模板

**Upwork Proposal 模板**（每次投递时快速修改）：

```
Hi [Client Name],

I'm a full-stack Next.js developer with hands-on experience building 
modern web applications with TypeScript, Prisma, and Supabase.

I've read your job description carefully, and here's how I'd approach 
your project:

1. [具体方案 1]
2. [具体方案 2]
3. [具体方案 3]

You can check out my portfolio here: [Vercel 链接]
My GitHub: [GitHub 链接]

I can start immediately and deliver within your timeline.

Looking forward to discussing this further!

Best,
[Your Name]
```

---

## 附录：检查清单

### TypeScript 迁移完成标志

- [ ] `jsconfig.json` 已删除
- [ ] `tsconfig.json` 已创建并配置严格模式
- [ ] `types/` 目录已创建，包含 `index.ts`、`mdx.d.ts`、`env.d.ts`
- [ ] 所有 `lib/` 文件迁移为 `.ts`
- [ ] 所有 `hooks/` 文件迁移为 `.ts`
- [ ] 所有 `components/` 文件迁移为 `.tsx`
- [ ] 所有 `app/` 页面迁移为 `.tsx`
- [ ] `next.config.ts` 已创建
- [ ] `mdx-components.tsx` 已迁移
- [ ] `npx tsc --noEmit` 零错误
- [ ] `npm run build` 成功
- [ ] 开发环境所有功能正常

### Prisma + Supabase 完成标志

- [ ] Supabase 项目已创建，环境变量已配置
- [ ] `prisma/schema.prisma` 已定义
- [ ] `npx prisma migrate dev` 成功
- [ ] `lib/prisma.ts` 已创建
- [ ] `lib/posts.ts` 已改为 Prisma 查询
- [ ] `project-list.tsx` 已改为 Prisma 查询
- [ ] `prisma/seed.ts` 已创建并运行
- [ ] API Route `/api/posts` 正常工作
- [ ] `db.json` 和 `json-server` 已移除
- [ ] Vercel 部署成功

### 平台注册完成标志

- [ ] Upwork 账号已注册，Profile 已完善
- [ ] Fiverr 账号已注册，Gig 已发布
- [ ] 国内平台已注册（可选）
- [ ] Vercel 线上 Demo 可访问
- [ ] GitHub Profile README 已优化
- [ ] Proposal 模板已准备
- [ ] 开始投递第一个 Proposal 🚀

---

## 时间线总览

```
Day 1 ─── 环境搭建 + 类型定义 + 第一批迁移 (lib)
Day 2 ─── 组件 + 页面迁移
Day 3 ─── 配置迁移 + 修复 + 验证通过
─────────────────────────────────────
Day 4 ─── Supabase 项目 + Prisma Schema + 数据迁移
Day 5 ─── API Routes + 清理 + Vercel 部署验证
─────────────────────────────────────
Day 6 ─── 平台注册 + Profile 完善 + 投递
─────────────────────────────────────
🎉 开始接单！
```

> **备注**：Day 4-5 的 Prisma + Supabase 集成实际上可以在 TypeScript 迁移过程中并行准备（创建 Supabase 项目、设计 Schema），但数据访问层的重写建议在 TS 迁移完成后再进行，避免同时改类型和逻辑导致排查困难。
